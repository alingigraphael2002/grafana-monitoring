apiVersion: 1

providers:
  - name: Observability demos
    orgId: 1
    folder: Observability
    folderUid: observability
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards/json
